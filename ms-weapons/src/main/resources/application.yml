server:
  port: 8082

spring:
  application:
    name: ms-weapon
  config:
    import: "optional:configserver:"
  datasource:
    url: jdbc:postgresql://localhost:5432/db_hitbox_stats_weapons
    username: postgres
    password: HitzonePassword123!
    driver-class-name: org.postgresql.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
eureka:
  client:
    serviceUrl:
      defaultZone: http://localhost:8761/eureka/
  instance:
    prefer-ip-address: true